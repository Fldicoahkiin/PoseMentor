version: "3.9"

services:
  posementor-demo:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "7860:7860"
    volumes:
      - ../data:/app/data
      - ../artifacts:/app/artifacts
      - ../outputs:/app/outputs
    environment:
      - PYTHONUNBUFFERED=1
